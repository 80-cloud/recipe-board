# CLAUDE.md — Claude Code 行動規範（recipe-board）

> このファイルは Claude Code が毎セッション必ず読み込み、厳守するルール集です。
> 記載のルールに反する操作（直接 main への push など）は、ユーザーに確認なく実行しないこと。
> 本ファイルは姉妹プロジェクト [hideharu-AI/Cursor (タスク管理アプリ)](https://github.com/80-cloud/hideharu-AI) の CLAUDE.md を**参考**に作成されており、レシピ管理アプリ独自の事情に合わせて改変している。

---

## ⚡ Quick Reference（速見表・最重要のみ）

> 本ファイルは長いため、**毎回必ず守るべき項目**だけを冒頭に集約。詳細は本文の各セクションへ。

### 守るべき 7 つのルール

1. **Issue ファースト**：作業前に Issue 起票 → ブランチ作成 → 実装 → PR → マージ（直接 main push 禁止）
2. **コミットメッセージ**：Conventional Commits 形式・日本語・50字以内（`feat:` / `fix:` / `docs:` / `chore:` 等）
3. **ブランチ名**：`feature/#番号-説明` / `fix/#番号-説明` / `docs/#番号-説明` / `chore/#番号-説明`
4. **PR**：日本語タイトル・テンプレート使用・`Closes #番号` でリンク・Squash and merge
5. **ポート**：Rails 3000 / Nuxt 3001 / MySQL 3306（競合時は kill して正規ポートで起動）
6. **AI 操作禁止**：`terraform destroy` / `terraform apply -auto-approve` / `aws *delete*` 等は人間承認必須
7. **`.env` などの機密情報を絶対にコミットしない**（`git status` で必ず事前確認）

### 作業中に頭をよぎったら止まる「Jidoka 発動条件」

- 「たぶん」「だと思う」「のはず」が頭をよぎった瞬間
- 「会話で出てきたから合ってるはず」と感じた瞬間
- ビルドエラー / テスト失敗が発生した瞬間
- ユーザーが「ちょっと待って」と言った瞬間

→ いずれも **書く手・実行を止め、`gh api` / `aws describe` / `curl` 等の実コマンドで裏取り**してから再開。

### 4 つの全プロジェクト共通教訓

1. ローカル設定値（git config / npm config 等）と外部サービスのアカウント名は別物
2. 思い込みより実コマンドでの検証（実行こそ真実）
3. 重要な固有名詞は記載前後に grep で一貫性確認
4. 仕組みが事故を防げなかった時、個人を責めず仕組みを直す

### 作業完了前の必須 3 ステップ（コードがある場合）

1. **コードレビュー**：自分のコードを他人として読み返す + 外部識別子の事実検証
2. **ビルド**：`npm run build` / `bundle exec rails test` がエラーなく完了
3. **動作確認**：localhost:3001 と localhost:3000/api/* が正常応答

→ 詳細は本文セクション 17 を参照。Setup フェーズ（コード無し）の代替手順は 17-4 へ。

---

## プロジェクト概要

| 項目 | 内容 |
|---|---|
| プロジェクト名 | レシピ管理アプリ (recipe-board) |
| リポジトリ | https://github.com/80-cloud/recipe-board |
| バックエンド | Ruby 3.x + Ruby on Rails 7.x（API モード） |
| フロントエンド | Nuxt 3（Vue.js 3 ベース）+ Tailwind CSS |
| データベース | MySQL 8.x（Docker コンテナ） |
| 本番デプロイ | AWS EC2 + RDS（無料枠） |
| 作業ディレクトリ | /Users/macmini/Desktop/recipe-board |
| 提出先 | スクール講師（Claude Code 利用は推奨されている） |

---

## 1. ブランチ命名規則

作業を始める前に必ず Issue を作成し、その番号をブランチ名に含めること。

| 種別 | 命名パターン | 例 |
|---|---|---|
| 新機能 | `feature/#(番号)-(短い説明)` | `feature/#1-requirements` |
| バグ修正 | `fix/#(番号)-(説明)` | `fix/#15-recipe-save-bug` |
| ドキュメント | `docs/#(番号)-(説明)` | `docs/#3-readme-update` |
| 雑務・設定 | `chore/#(番号)-(説明)` | `chore/#2-setup-eslint` |

**禁止事項:**
- `main` ブランチへの直接 push は絶対禁止
- Issue 番号のないブランチ名は作成しない
- `master` ブランチは使用しない

---

## 2. Issue ファースト・ワークフロー

**作業を始める前に必ず GitHub Issue を作成すること（または既存 Issue を確認すること）。**

```
① GitHub で Issue を作成する（テンプレートを使用）
         ↓
② Issue 番号を確認する（例: #1）
         ↓
③ ブランチを作成する: git checkout -b feature/#1-説明
         ↓
④ 作業・コミットを行う
         ↓
⑤ PR を作成し、Issue を Closes #1 でリンクする
         ↓
⑥ main へマージ後、ブランチを削除する
```

---

## 3. コミットメッセージ規則

**すべてのコミットメッセージは Conventional Commits 形式で、日本語で書くこと。**

### フォーマット

```
種別: 変更の要約（日本語・50文字以内）

詳細説明（任意・72文字で折り返す）
関連する背景・理由・注意点などを書く。

Closes #(Issue番号)  ← 該当する場合のみ
```

### 種別一覧

| 種別 | 用途 |
|---|---|
| `feat` | 新機能の追加 |
| `fix` | バグの修正 |
| `docs` | ドキュメントのみの変更 |
| `style` | コードの動作に影響しない変更（フォーマット等） |
| `refactor` | バグ修正でも機能追加でもないコード変更 |
| `test` | テストの追加・修正 |
| `chore` | ビルドツールや補助ツールの変更 |

### コミットメッセージ例

```
feat: レシピの登録・一覧・詳細表示機能を追加

Rails API 側で Recipe モデルと CRUD エンドポイントを実装。
Nuxt 側でレシピ一覧画面とレシピ詳細画面を作成した。

Closes #5
```

---

## 4. プルリクエスト (PR) 規則

- **タイトルは日本語で書くこと**
- **必ず `.github/pull_request_template.md` のテンプレートを使用すること**
- **必ず関連 Issue を `Closes #番号` で本文にリンクすること**
- **main ブランチへのマージ方法: Squash and merge（スカッシュマージ）を使うこと**
- PR のタイトルはコミットメッセージと同じ形式にすること: `種別: 説明`

---

## 5. ラベル運用ルール

Issue・PR には必ずラベルを付けること。

| ラベル | 色 | 用途 |
|---|---|---|
| `enhancement` | 水色 | 新機能・改善 |
| `bug` | 赤 | バグ報告・修正 |
| `documentation` | 青 | ドキュメントのみの変更 |
| `chore` | グレー | 設定・ツール・依存関係の変更 |

---

## 6. 禁止事項（Claude Code が守るべきルール）

1. `git push origin main` を直接実行しない
2. `git push --force` を main に対して実行しない
3. Issue なしにブランチを作成しない
4. 英語でコミットメッセージを書かない（日本語必須）
5. PR なしに main へのコードを反映しない
6. `.env` ファイルを Git にコミットしない
7. データベースのパスワードをコードに直書きしない
8. **`terraform destroy` を本人の明示承認なしに実行しない**（特に auto-approve禁止）
9. **`terraform apply -auto-approve` を本人の明示承認なしに実行しない**（破壊的変更が含まれる可能性）
10. **`aws *delete*` `aws *terminate*` `aws s3 rb*` を本人の明示承認なしに実行しない**
11. **本番データベース・バックアップを削除する操作は、AI 単独で完結させない**（必ず人間の承認をはさむ）
12. **タスク管理アプリ (hideharu-AI/Cursor) の AWS リソースに触らない**（同一 AWS アカウント内に存在する場合）

---

## 7. 技術スタック補足（Claude Code 向け）

### サービス起動（起動順を守ること）

```bash
# ① MySQL を Docker で起動
cd /Users/macmini/Desktop/recipe-board
docker-compose up -d

# ② Rails を起動（バックエンド）
cd /Users/macmini/Desktop/recipe-board/backend
bundle install
rails db:migrate
rails s -p 3000

# ③ Nuxt フロントエンドを起動
cd /Users/macmini/Desktop/recipe-board/frontend
npm install
npm run dev  # デフォルトで 3001 番ポートで起動するよう設定
```

> ポート競合が発生した場合はセクション 10 のルールに従うこと。

### ファイル構成（予定）

```
/Users/macmini/Desktop/recipe-board/
├── CLAUDE.md                    ← 本ファイル（必読）
├── README.md
├── .gitignore
├── .github/
│   ├── ISSUE_TEMPLATE/
│   │   ├── bug_report.md
│   │   └── feature_request.md
│   └── pull_request_template.md
├── docs/                        ← 設計ドキュメント
│   ├── 要件定義書.md
│   ├── 画面設計書.md
│   ├── DB設計書.md
│   └── 技術スタック.md
├── docker-compose.yml           ← MySQL 起動
├── backend/                     ← Rails (Ruby 3.x, API モード)
│   ├── Gemfile
│   ├── config/
│   └── app/
│       ├── controllers/api/
│       ├── models/
│       └── services/
└── frontend/                    ← Nuxt 3 (Vue.js 3)
    ├── package.json
    ├── nuxt.config.ts
    ├── pages/
    ├── components/
    └── composables/
```

---

## 8. GitHub ブランチ保護設定（セットアップ用コマンド）

リポジトリ作成後、以下を実行して保護設定を入れる。

```bash
gh api repos/80-cloud/recipe-board/branches/main/protection \
  --method PUT \
  --header "Accept: application/vnd.github+json" \
  --input - <<'EOF'
{
  "required_status_checks": null,
  "enforce_admins": false,
  "required_pull_request_reviews": {
    "dismiss_stale_reviews": true,
    "require_code_owner_reviews": false,
    "required_approving_review_count": 0
  },
  "restrictions": null,
  "allow_force_pushes": false,
  "allow_deletions": false
}
EOF
```

> 個人リポジトリ（GitHub Free）では PR レビューの必須化ができない場合がある。
> その場合は `"required_pull_request_reviews": null` に変更し、CLAUDE.md のルールで運用カバーする。

---

## 9. 検証方法

セットアップ後に以下のコマンドで確認すること。

```bash
# 1. CLAUDE.md の内容確認
cat CLAUDE.md

# 2. ブランチ保護が有効か確認
gh api repos/80-cloud/recipe-board/branches/main/protection | python3 -m json.tool

# 3. ラベル一覧確認
gh label list --repo 80-cloud/recipe-board
```

---

## 10. ポート競合の扱い（Claude Code 必須ルール）

### 使用ポート一覧

| サービス | コマンド | ポート |
|---|---|---|
| Rails（バックエンド） | `rails s -p 3000` | **3000** |
| Nuxt（フロントエンド） | `npm run dev` | **3001** |
| MySQL（DB） | `docker-compose up` | **3306** |

> **タスク管理アプリ (Cursor) のポート（8080 / 5173 / 5432）と重複しないように設定済み**。
> ただし、Rails と Nuxt は両方ともデフォルトで 3000 番を使うため、Nuxt 側を 3001 に明示的に変更すること。

### ルール

1. **サーバー起動前に必ずポートの空きを確認すること**
2. **ポートが使用中の場合は、そのプロセスを `kill -9` で強制終了してから起動すること**
3. **別ポートでの代替起動は絶対禁止** — CORS 設定・Nuxt プロキシが崩れてアプリが動作しない

### 強制終了コマンド（手動で実行する場合）

```bash
# ポート 3000 を解放（Rails）
lsof -ti :3000 | xargs kill -9 2>/dev/null; true

# ポート 3001 を解放（Nuxt）
lsof -ti :3001 | xargs kill -9 2>/dev/null; true

# ポート 3306 を解放（MySQL）
lsof -ti :3306 | xargs kill -9 2>/dev/null; true
```

---

## 11. このファイルの更新ルール

- ルールを変更したい場合は、必ず Issue を立ててから PR 経由で変更すること
- 直接編集してコミットしない
- 変更時のコミットメッセージ: `chore: CLAUDE.md のルールを更新 (#番号)`

---

## 12. AI 誤操作防止の多層防御ルール

AWS / Terraform を扱う AI コーディングツールが本番環境を破壊する事故が業界で複数報告されているため、本リポジトリでも **多層防御** で予防する。詳細はタスク管理アプリの CLAUDE.md セクション 12 を参照。

### AI が単独で実行してはならない操作

- `terraform destroy` 全般
- `terraform apply -auto-approve`（明示承認なしの自動適用）
- `terraform state rm` / `terraform workspace delete`
- `aws rds delete-*` / `aws ec2 terminate-*` / `aws s3 rb`
- `aws iam delete-*`（権限・ロール削除は復旧困難）
- 本番データに対する手動 SQL（`DROP TABLE` / `TRUNCATE` 等）

### タスク管理アプリとの AWS 無料枠共有について

**重要:** タスク管理アプリと本アプリは同一 AWS アカウントを使用するため、無料枠（EC2 750h / 月、RDS 750h / 月）を共有する。

- 両アプリの EC2 / RDS を**同時に起動しっぱなし**にすると、月の途中で無料枠を超過し課金が始まる
- 開発時の運用ルール: **使わない方の EC2 / RDS は停止しておく**
- 提出期間中はレシピアプリのみ起動、タスク管理アプリは停止する

---

## 13. AI 利用に関する方針（提出物として）

本プロジェクトは Claude Code を活用して開発する。スクール講師から AI 利用は推奨されている。

### 透明性の確保

- 本リポジトリでの AI 利用は README に明記する
- コミットメッセージに `Co-Authored-By: Claude` を残す（自動付与）
- 設計判断・技術選定の議論は AI と相談して進めるが、**最終判断は人間が行う**

### 学習姿勢

- AI に書かせたコードは、**自分の言葉で説明できる状態**にしてからコミットする
- 分からないコードは AI に「なぜこの書き方？」と質問する
- AI の説明が公式ドキュメントとズレていたら、**公式が正しい**

---

## 14. 学習方針（recipe-board 固有）

本プロジェクトはスクールでの学習・提出が目的のため、以下の方針で進める。

### 14-1. 「現場の流れ」を踏襲する

- Issue → Branch → PR → Review → Merge の順序を例外なく守る
- コミットメッセージは Conventional Commits 形式・日本語
- ドキュメント（要件定義書 / 画面設計書 / DB設計書）を提出物として整備する

### 14-2. 「やったことがないこと」をやる（講師の指示）

- タスク管理アプリで使用済みの **Java / PostgreSQL / React は使用しない**
- 本プロジェクトでは Ruby / Rails / Vue.js (Nuxt) / MySQL を採用

### 14-3. 完成優先・スコープ最小化

- MVP（最小機能）：レシピの CRUD、材料リスト、手順リスト
- Phase 2（時間が許せば）：タグ機能、検索、画像アップロード
- 提出を優先し、機能追加は完成後に行う

### 14-4. 1機能を縦に貫通させる

- 横展開（画面だけ全部 / DB だけ完璧）は避ける
- 1機能（例: レシピ登録）を DB → API → 画面まで縦に通してから次の機能へ

---

## 15. 全プロジェクト共通教訓と振り返り

> **注意（リポジトリ閲覧者向け）:**
> 本プロジェクトは開発者個人の **20 アプリ開発計画** の一部であり、
> 開発者の手元 PC には全プロジェクト共通の学習リソース（横断学習システム）が併用されている。
> ただし**本リポジトリは単独で完結する仕様**であり、外部リソースが無くても運用可能。
> 横断システムの詳細は `OPERATIONS.md`（本リポジトリの運用方針）を参照。

### 15-1. 全プロジェクト共通教訓（最高レベル）

過去の事故・経験から抽出した、すべての作業に適用すべき原則：

1. **ローカル設定値（git config / npm config / aws config 等）と外部サービスのアカウント名は別物として扱う**
2. **思い込み・会話の流れで進めず、必ず実コマンドで真偽を確定させる（実行こそ真実）**
3. **重要な固有名詞（URL / アカウント名 / リソース名 / ARN）は記載前後に grep + 実コマンドで一貫性を確認する**
4. **仕組みが事故を防げなかった時、個人を責めずに仕組みを直す（PDCAS の S は組織のシステムを対象とする）**

### 15-2. タスク管理アプリ（前プロジェクト）からの教訓継承

前プロジェクト（hideharu-AI/Cursor タスク管理アプリ）で起きた事故事例（Claude Code が Terraform で本番環境を全削除した件）の教訓を本プロジェクトでも継承する。

- バックアップは必ずクロスアカウントで保管（将来的に）
- `prevent_destroy` / `deletion_protection` を必ず設定
- AI が「クリーンアップして」と言われた場合の解釈に注意
- 便利さ（auto-approve）と安全性のトレードオフを意識する

### 15-3. 本プロジェクト完了時の振り返り

最終 PR マージ後に以下を実施する：

- [ ] `docs/learning-notes.md` を読み返す
- [ ] 抽象化できる学びを抽出する
- [ ] 発生した事故があれば詳細を記録する
- [ ] CLAUDE.md の改善点を次プロジェクト用にメモする

> 横断学習システム（事故ライブラリ・教訓ライブラリ・振り返りスケジュール）は
> 開発者の手元 PC に別途配置されており、本振り返り結果はそこに反映される。
> 本リポジトリの運用には影響しない。

---

## 16. 学習サイクル：トヨタ式 PDCA(S) を採用する

本プロジェクトは**学習目的**のため、すべての作業サイクルにトヨタ式 PDCA を適用する。
通常の PDCA との最大の違いは **S（標準化）** が加わり **PDCAS** になること、
そして問題の根本原因を **5 Whys（なぜなぜ分析）** で徹底追求すること。

> **Why（なぜ採用するか）:**
> 「動いた」で終わらせず、**学んだことを次に活かす仕組み**を持つため。
> 不具合の再発防止と、良い成果の再現・横展開によって、レシピアプリ全体の品質と
> 自分のスキルを継続的に向上させる。

> **How（どう適用するか）:**
> 作業の種類（通常実装・バグ対応・良い成果）ごとに以下のサイクルを回す。
> 最低でも各機能の Issue 完了時に「S（標準化）」を実施し、学びを残す。

---

### 16-1. トヨタ式 PDCA の核心概念

| 概念 | 意味 | recipe-board での適用例 |
|---|---|---|
| **PDCAS** | Plan→Do→Check→Act→**Standardize** | 良い手順・知見は必ず CLAUDE.md または `docs/learning-notes.md` に記録 |
| **5 Whys（なぜなぜ分析）** | 「なぜ？」を5回繰り返して根本原因を追求 | バグ発生時、表面的な修正で終わらせない |
| **三現主義** | 現場・現物・現実を自分の目で確認 | バグを自分で再現し、実際のコードを読み、ログを見る |
| **自働化（Jidoka）** | 問題を発見したら即停止・前に進まない | ビルドエラー・テスト失敗時はコミット/PR 作成を止める |
| **カイゼン** | 小さな改善を継続的に積み重ねる | 一度に大きく変えず、小さく安全に変更を重ねる |

---

### 16-2. サイクル1: 通常の実装作業（Issue ごと）

```
P（計画）: Issue 作成 → 受け入れ条件を明文化 → 想定リスクを書き出す
D（実行）: 安心・安全ファーストで実装（問題を見つけたら Jidoka で即停止）
C（確認）: コードレビュー → ビルド成功 → ローカル動作確認
A（処置）: 問題があれば 5 Whys で根本原因を特定して修正
S（標準化）: 学んだ手順・知見を CLAUDE.md または docs/learning-notes.md に記録
```

> **サブタスクごとに P を回す**（2026-05-08 のアカウント名誤認事故を受けて追記）：
> 1 つの Issue 内でも、ファイル作成・コマンド実行・URL 記載などの単位で「受け入れ条件」を明文化する。
> **固有名詞（URL・アカウント名・リソース名）は受け入れ条件に必ず含めて検証可能にする**。
> 例: 受け入れ条件「README.md の GitHub URL が `gh api user` の出力値と完全一致する」

---

### 16-3. サイクル2: バグ・不具合発生時（最重要）

```
P（計画）: 三現主義でバグを自分で再現し、影響範囲を特定
D（実行）: 最小限の変更で修正（他への影響を最小化）
C（確認）: 修正後にビルド・動作確認・既存機能の確認
A（処置）: なぜなぜ分析（5 Whys）で根本原因を特定
S（標準化）: 再発防止策をルールとして CLAUDE.md または docs/learning-notes.md に追記
```

#### なぜなぜ分析テンプレート（バグ対応時に必ず書く）

```
問題:   レシピが保存できない
なぜ1: API リクエストが 422 エラーを返している
なぜ2: バリデーションエラーで弾かれている
なぜ3: 材料の name が空文字でも送信されている
なぜ4: フロント側で空文字チェックをしていない
なぜ5: 受け入れ条件にエッジケース（空入力）を含めていなかった
→ 対策: Issue 作成時に必ずエッジケースを受け入れ条件に書く（ルール化）
```

---

### 16-4. サイクル3: 良い成果・うまくいった作業

```
C（確認）: 何がうまくいったか・なぜうまくいったかを言語化する
A（処置）: 再現性のある手順として整理する
S（標準化）: CLAUDE.md または docs/learning-notes.md に記録し、次の作業へ横展開
```

> 「成功した作業ほど標準化を忘れる」のがアンチパターン。
> 良い成果が出た時こそ S を必ず行う。

---

### 16-5. 判断基準（Jidoka の発動条件）

| 状況 | 対応 |
|---|---|
| ビルドエラー / テスト失敗 | **Jidoka 発動**：即停止・5 Whys で根本原因を追ってから再開 |
| ローカルでアプリが動かない | **Jidoka 発動**：原因を特定するまで次の作業に進まない |
| 軽微な警告（lint warning 等） | 記録して次のカイゼンサイクルで対処 |
| 良い成果が出た | 必ず標準化（S）を実施してから次の作業へ |
| AI が出した提案が「動いたけど理由が分からない」 | **Jidoka 発動**：「なぜ動くか」を自分の言葉で説明できるまで止まる |

---

### 16-6. 学習ノートの運用ルール

`docs/learning-notes.md` に学びを継続的に記録する。形式は自由だが、最低限以下を含める：

```markdown
## YYYY-MM-DD: [作業内容のタイトル]

### 何をしたか
（実施した作業を1〜2文で）

### つまずいた点 / 疑問
（バグ・エラー・分からなかったこと）

### 5 Whys（バグの場合）
（テンプレートに沿って書く）

### 学んだこと
（標準化＝S の内容。次回も使える知見）

### 横展開できそうなこと
（他の Issue / プロジェクトに応用できる学び）
```

> 記録のハードルを下げる：完璧を狙わず、**1作業1メモ**を目標に書き続ける。
> 後から見返した時に「なぜそう判断したか」を思い出せる粒度であれば十分。

---

### 16-7. recipe-board での具体的なフロー例

例: **「レシピ登録 API を実装する」Issue を進める場合**

```
P: Issue #5 起票
   - 受け入れ条件: title / 材料 / 手順 を保存できる
   - エッジケース: title 空、材料 0個、手順 0個
   - リスク: バリデーション漏れ、N+1 問題

D: feature/#5-recipe-create-api ブランチで実装
   - models/recipe.rb 作成 → Jidoka チェック（rails s でエラーなし）
   - controllers/api/recipes_controller.rb 作成 → Jidoka チェック
   - 都度 curl で動作確認（三現主義）

C: ローカル動作確認
   - 正常系: curl POST で 201 Created が返る
   - 異常系: title 空で 422 が返る
   - rails test 通過

A: 問題があれば 5 Whys → 修正

S: 学んだことを docs/learning-notes.md に記録
   例: 「Strong Parameters は params.require(:recipe).permit(...) の形」
       「ネストした材料の保存は accepts_nested_attributes_for を使う」

→ PR 作成 → マージ → 次の Issue へ
```

---

## 17. 安心・安全ファースト作業方針

すべての実装・変更作業は「安心・安全」を大前提に進め、完了後に必ず以下の **3 ステップ** を実施する。

> **Why（なぜ採用するか）:**
> バグや不具合を未然に防ぐため。Panasonic の基本理念「安心・安全」を作業指針として採用したいというユーザーの方針を、タスク管理アプリから本プロジェクトへ継続適用する。
>
> **How（どう適用するか）:**
> 実装が終わったら、**PR を出す前**（または「完了しました」と報告する前）に必ず下記の 3 ステップを実行する。セクション 16 の Jidoka と組み合わせて、エラーや警告が出た場合は即停止して原因を特定する。

---

### 17-1. 作業完了後の必須チェック3ステップ

#### ステップ 1: コードレビュー（品質・安全性確認）

- 変更した全ファイルを読み直し、バグ・ロジックミス・セキュリティリスクがないか確認
- デバッグ文の残留を確認（`console.log` / `puts` / `pp` / `binding.pry` / `byebug` / `debugger` 等）
- 未使用の変数・インポート・gem を確認
- ハードコードされた値（API URL・認証情報・マジックナンバー）がないか確認
- エラーハンドリングの抜け漏れを確認
- **`.env` などの機密情報が誤ってコミット対象に含まれていないか**を `git status` で必ず確認
- **外部識別子の事実検証**（2026-05-08 のアカウント名誤認事故を受けて追加）：
  - GitHub アカウント名・リポジトリ URL は `gh api user --jq '.login'` / `gh repo view <owner>/<repo>` で実値と一致確認
  - AWS リソース名・ARN は `aws ec2 describe-*` / `aws rds describe-*` で実存確認
  - API エンドポイント URL は実際に `curl` で疎通確認
  - 「思い込み」「会話の流れ」での記載を禁止し、**必ず実コマンドで裏取り**
  - ファイル作成後、`grep -rn <識別子> .` で全ファイルの一貫性を確認

#### ステップ 2: チェックサム（ビルド検証）

| 対象 | コマンド |
|---|---|
| フロントエンド（Nuxt）ビルド | `cd frontend && npm run build` |
| フロントエンド lint | `cd frontend && npm run lint`（設定後） |
| バックエンド（Rails）テスト | `cd backend && bundle exec rails test`（テスト存在時） |
| バックエンド lint | `cd backend && bundle exec rubocop`（設定後） |
| マイグレーション整合性 | `cd backend && bundle exec rails db:migrate:status` |

→ いずれも **エラーなく完了することが必須**。エラーが出たら Jidoka 発動・即停止。

#### ステップ 3: ヘルスチェック（動作確認）

- フロントエンド `http://localhost:3001` が表示される
- バックエンド `http://localhost:3000/api/recipes`（または該当エンドポイント）が正常な JSON を返す
- 変更に関連する主要機能が画面上で動作する（UI 変更の場合は **必ずブラウザで実操作**）
- ブラウザ Console / サーバーログ（`backend/log/development.log`）にエラー・警告が出ていない
- DB に意図しない更新・削除が起きていないか（必要に応じて MySQL に接続して確認）

---

### 17-2. PDCA との関係

セクション 16 のトヨタ式 PDCA(S) における **D（実行）と C（確認）の段階で、本セクションのチェックを実施する**。

```
D（実行）: 安心・安全ファースト（17-1）を意識しながらコードを書く
            問題を見つけたら Jidoka で即停止
C（確認）: 17-1 の 3 ステップを順に実行
            ステップ 1: コードレビュー
            ステップ 2: チェックサム（ビルド）
            ステップ 3: ヘルスチェック（動作確認）
```

「面倒だから後で」とヘルスチェックや動作確認を省略しない。
**省略はバグ流出の最大原因**であり、これを徹底するためにこのセクションを存在させている。

---

### 17-3. 学習者向けの補足（recipe-board 固有）

学習中は「動いた！」で満足しがちだが、**動いたあとに 3 ステップを必ず行う**ことを習慣化する。

- **コードレビュー**は「自分が書いたコードを他人として読み返す」訓練になる
- **チェックサム（ビルド）**は「ローカルで動いても本番で壊れる」を防ぐ最初の砦
- **ヘルスチェック**は **三現主義（セクション 16-1）** の実践そのもの

3 ステップをルーチン化することで、「やってないのに完了報告」を構造的に防ぐ。

---

### 17-4. Setup フェーズ用チェック（コード無し時）

**追加経緯**: 2026-05-08 にプロジェクト初期セットアップ中、GitHub アカウント名を誤認する事故が発生（詳細は `docs/learning-notes.md` 参照）。
17-1 の通常 3 ステップは「コードが既に動いている」前提のため、**setup フェーズでは適用不能**であることが判明した。
本セクションは setup フェーズ専用の代替チェック手順として追加。

#### 17-4-1. Setup フェーズの定義

以下のいずれかに該当する状態：

- まだバックエンド/フロントエンドのコードを書いていない（雛形のみ）
- ドキュメント・設定ファイル（CLAUDE.md / README.md / .gitignore / Terraform / docker-compose.yml 等）の作成・編集中
- `bundle install` / `npm install` 等の依存解決が未完了
- ローカルで `localhost:XXXX` にアクセスできない

#### 17-4-2. Setup フェーズ用 3 ステップ（コード無し版）

通常の「コードレビュー・ビルド・ヘルスチェック」の代わりに：

##### ステップ A: 固有名詞の grep 検証

```bash
# 重要な固有名詞をすべて抽出
grep -rn -E "(github\.com/[^/]+/[^/[:space:]]+|aws.*amazon\.com|/Users/[^/[:space:]]+|localhost:[0-9]+)" . --include="*.md" --include="*.yml" --include="*.yaml" --include="*.tf"

# 抽出された値を一つずつ実コマンドで裏取り
# 例: GitHub URL → gh repo view <owner>/<repo>
# 例: AWS ARN  → aws iam get-role --role-name ...
```

##### ステップ B: 三現主義の実践（実コマンドでの値取得）

ファイルに記載した外部識別子について、**会話・推測ではなく実コマンドで取得した値**かを確認：

| 種類 | 実コマンド | 検証先 |
|---|---|---|
| GitHub アカウント名 | `gh api user --jq '.login'` | GitHub |
| GitHub リポジトリ | `gh repo view <owner>/<repo> --json url` | GitHub |
| AWS リソース | `aws ec2/rds/iam describe-*` | AWS |
| API エンドポイント | `curl -I <URL>` | 該当サービス |
| ローカルポート占有 | `lsof -ti :<port>` | ローカル OS |

##### ステップ C: Pre-flight Check の再走

開発者の手元にある Pre-flight Check リスト（外部リソース）を上から再度確認。
特に **自己診断質問**（その値の根拠は何か？実コマンドの出力か？）と
**GitHub 認証確認**（`gh auth status` / `gh api user --jq '.login'`）を必ず実行。

> ※ 本リポジトリ閲覧者向け補足: 開発者個人の横断学習システムを参照する手順だが、
> 本リポジトリ単独でも `gh` コマンド等で同等の検証は可能。

#### 17-4-3. Setup フェーズで Jidoka を発動する条件

通常の Jidoka はビルドエラー等の "技術的シグナル" を待つが、setup フェーズではシグナルが出ない。
代わりに以下の "状況的シグナル" で Jidoka を発動する：

- ⚠️ ファイルに記載しようとしている固有名詞（URL / アカウント名 / リソース名）の**根拠が会話の流れだけ**である
- ⚠️ 「たぶん」「だと思う」「のはず」と内心で考えた瞬間
- ⚠️ 同じ識別子を複数のファイルに繰り返し書いているが**一度も実コマンドで検証していない**
- ⚠️ ユーザーが「ちょっと待って」「確認して」と言った時

→ 上記いずれかに該当したら、**書く手を止めて実コマンドで裏取り**してから再開する。

